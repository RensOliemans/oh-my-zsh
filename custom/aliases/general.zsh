alias py='python3.7'

# Call ipython from within the virtualenv
alias ipy="python -c 'import IPython; IPython.terminal.ipapp.launch_new_instance()'"

alias start-latex='git clone --depth=1 --branch=master git@gitlab.com:RensOliemans/latex-template && rm -rf latex-template/.git'
